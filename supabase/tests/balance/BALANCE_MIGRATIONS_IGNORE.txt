# Migrations that touch balance-related tables (expenses / custom_payment_sources
# / app_settings) but are intentionally EXCLUDED from balance-sql-suite.yml.
#
# Each entry MUST be justified in the comment. Drift-check
# (supabase/tests/balance/detect-drift.sh) reads this file to permit changes
# to balance-relevant tables that do not affect balance semantics
# (e.g. RLS-only, GRANT-only, or unrelated column additions).
#
# Format: one glob per line. Blank/comment lines skipped.

# Pre-existing balance-adjacent migrations (grandfathered — pre-date the gate).
# All balance-neutral: neither touches trigger, recompute function, anchor
# columns, event_at semantics, nor engine mode.
20260605143443_*.sql  # E2E: profiles flag + RPC (accidentally matches "expenses" in a comment)
20260608150431_*.sql  # DROP unused expenses.is_private / split_overrides
20260618193820_*.sql  # CHECK constraint on expenses.payment_source string format
20260621145942_*.sql  # Add expenses.recurring_transaction_id FK (no balance impact)
20260628163350_*.sql  # Relax NOT NULL on event_at — trigger still populates

# Manual ↔ bank merge writer — covered by its OWN gate
# (.github/workflows/merge-sql-suite.yml + supabase/tests/merge/).
# Balance-neutralan: uklanja ručnu balance aritmetiku iz RPC-a i prepušta
# saldo isključivo triggeru; harness dokazuje da spajanje makne točno jedan
# učinak (unanchored i anchored slučaj).
20260824190123_*.sql  # merge_manual_with_bank: identitet se seli, bez dvostrukog reverta

# Isplata s kartice čovjeka: create_person_payout + create_worker_payout dobiva
# dijeljeni expense/batch. Upis u expenses ide postojećim putem (isti trigger,
# ista anchor semantika) — balance-neutralno.
20260825213455_ca634f91-c873-4561-9306-b3e47e74a022.sql
